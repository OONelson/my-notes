# Database — from Neon dashboard → Connection string (direct, no -pooler hostname)
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require

# Auth
JWT_SECRET=<run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))">
JWT_EXPIRES_IN=7d

# CORS — set to your Vercel frontend URL after deploying
CORS_ORIGIN=https://not-lify.vercel.app

# Redis — optional, from Upstash free tier (token blacklisting on logout)
REDIS_URL=

# Google OAuth — optional, only if using Google sign-in
GOOGLE_CLIENT_ID=
